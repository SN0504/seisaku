第一案
--------------------------------------------
#include <stdio.h>
int main(void){
    int n,k,i,j,m,num;
    int cnt = 0;
    int snow[100000];
    scanf("%d %d",&n,&k);
    for(i = 0;i < n;i++){
        scanf("%d",&snow[i]);
    }
    for(i = 0;i < n;i++){
        m = snow[i];
        for(j = i;j < n;j++){
            if(m < snow[j]){
                m = snow[j];
                num = j;
            }
        }
        if(snow[i] < m){
            snow[num] = snow[i];
            snow[i] = m;
        }
    }
    num = n;
    for(i = 0;i < j - 1;i++){
        for(j = num;j > i;j--){
            if(snow[i] + snow[j] >= k){
                cnt++;
                num = j - 1;
                break;
            }
        }
    }
    
    printf("%d\n",cnt);
}
------------------------------------------
最後だけ出力結果が間違っているため90点。理論上は間違っていないはず？
