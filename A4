第一案
なんでAなのかがわからないくらい単純なソート問題。正直大して書くことがないです。
-------------------------------------------
#include <stdio.h>
int main(void){
    int cnt = 0,i,j,n,m,num; //カウント用の変数だけは初期化しておかないとだめ
    int data[100000];
    scanf("%d",&n);
    for(i = 0;i < n;i++){
        scanf("%d",&data[i]);
    }
    // 左から一冊ずつ本を比較し入れ替えていく
    for(i = 0;i < n;i++){
        m = data[i]; //i番目の本の数字を記憶しておく
        for(j = i;j < n;j++){
            if(m > data[j]){
                m = data[j]; //もしj番目の数字が記憶されている数字よりも小さかった場合その数字を記憶する
                num = j; //本の数字だけでなく何番目の本であったかも記憶する
            }
        }
        // もし記憶していた数字がi番目の本よりも小さかった場合i番目のほんとj番目の本を入れ替える
        if(data[i] > m){
            data[num] = data[i];
            data[i] = m;
            cnt++; //入れ替えが必要になった場合のみカウントを増加させる
        }
    }
    printf("%d",cnt);
    return 0;
}
-----------------------------------------
問題なくクリア
